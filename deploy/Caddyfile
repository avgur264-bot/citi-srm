# Адрес сайта подставляется из переменной SITE_ADDRESS (файл .env):
#   :80                     — работа по IP-адресу сервера (без HTTPS)
#   srm.вашакомпания.ru     — Caddy сам выпустит и продлит бесплатный SSL-сертификат
{$SITE_ADDRESS} {
    encode gzip
    reverse_proxy app:4000
}
